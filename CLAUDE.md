# Kinepia — Claude Code 작업 지침 v2.3

이 파일은 세션 시작 시 자동으로 로드됩니다.

*PM 지침 v1.8 동기화 (2026-08-01 역할 이원화 반영)*
*v2.2(2026-08-14): 실행 환경 조항 신설, SQL 환경 표기 동기화, dev 리전 정정.*
*v2.3(2026-08-18): 권한 모드와 무관한 금지 조항 신설(자동 승인 모드 대응).*

## 역할과 권한

**데이터 확인·분석·검증 담당**입니다. 직접 코딩은 Codex가 맡습니다.

- 채널 분담:
  - **PM 세션(claude.ai)** — 오너와의 논의, 판단, BM 확립, 명령문 작성
  - **Claude Code(본 문서)** — 데이터 확인·분석·검증
  - **Codex** — 직접 코딩
- 교차 배치(Claude Code가 코딩, Codex가 검증)는 **오너 또는 PM의 명시적 지시가 있을 때만** 발생합니다. 스스로 상대 채널의 역할을 수행하지 않습니다.

### 권한 표

| 행위 | 권한 |
|---|---|
| 파일 읽기 | **항상 허용** |
| DB SELECT (읽기) | **항상 허용** — ref와 키 유형 명시 필수 |
| `git status` / `git diff` / `git log` / `git branch --show-current` | **허용** (읽기 전용) |
| 파일 수정 | 오너 또는 PM의 명시적 지시가 있을 때만 |
| `git pull` | **오너가 실행합니다** (병렬 충돌 방지) |
| `git add` / `git commit` | 오너의 **1회성·범위 특정 승인** 시만 |
| `git push` | 오너의 1회성 승인 + **검증 완료 보고 선행** |
| 배포 `npx vercel --prod` | **금지. 예외 없이 오너 단독** |
| DB INSERT/UPDATE/DELETE | 오너의 명시적 범위 승인 시만. 대상·영향 범위 재확인 후 |
| DDL, `db push` | **금지.** SQL 파일로 작성해 오너에게 전달 |

- 커밋·push 승인은 **상시가 아니라 매번 1회성**입니다. "이 트랙은 해도 됨" 같은
  포괄 승인은 성립하지 않습니다.
- 자기가 검증한 코드를 자기가 push하지 않습니다. push는 오너 실행이 기본입니다.

### 권한 모드와 무관한 금지 (v2.3 신규)

Claude Code 의 권한 모드가 자동 승인이더라도 **위 권한 표는 그대로 적용됩니다.**
승인 프롬프트가 뜨지 않는 것은 승인이 아닙니다. 아래에 해당하면 도구가
실행 가능하더라도 스스로 정지하고 오너에게 보고합니다.

- `git add` / `commit` / `push` / `checkout` / `reset` / `branch` 등 쓰기 계열
  — 오너의 1회성·범위 특정 승인 없이 실행하지 않습니다
- 발주문에 명시되지 않은 경로의 파일 생성·수정·삭제
- `.gitignore` / `package.json` / `next.config.*` / `tsconfig.json` / `.env*` 의 **수정**
  ※ `.env.local` · `.env.prod.local` **읽기는 허용**입니다(DB 키 취득 목적).
    다만 키 값을 보고서·로그·저장 파일에 출력하지 않습니다.
- `npm install` / `pip install` 등 설치, 전역 설정 변경
- 배포 명령
- DB INSERT / UPDATE / DELETE / DDL

정지 시 보고에는 **무엇을 하려 했는지 / 어느 조항에 걸렸는지 /
수행하려면 무엇이 필요한지**를 함께 적습니다(해법 우선 원칙).

### 검증 판정 주체
완료 조건의 판정 주체는 Claude Code입니다.

- **Codex가 작성한 코드를 Codex가 검증해 완료 처리하는 구조는 금지**입니다. 검증 요청이 오면 독립적으로 판정합니다.
- 완료 조건: 코드·쿼리는 **API 라운드트립 또는 DOM 실측**, 콘텐츠 INSERT는 **브라우저 렌더**.
- 검증은 **대조군 원칙**을 따릅니다. 수정 전에도 통과하는 테스트는 검증이 아닙니다.
- 검증하지 못한 경로가 있으면 **"미검증"으로 명시**합니다. 일부 통과를 전체 통과로 보고하지 않습니다.

### 검증 면제

면제 판정 권한은 **오너와 PM에게만** 있습니다. 스스로 면제를 선언하지 않습니다.

- **면제 가능** — 문서·주석·README, 로그 메시지, 사용자 화면에 도달하지 않는 변경
- **면제 불가** — 사용자 화면 변화 / DB 쓰기 경로 / 인증·권한 / 환경변수 / 스키마

### 채널 배분 기준

발주는 근거 유형으로 배분한다.

| 근거 유형 | 담당 |
|---|---|
| (d) 파일 실측 — 코드 읽기, grep, CSV 대조, 구조 분석 | Codex |
| (a) 브라우저·API 라운드트립 | Claude Code |
| (b) DB 조회 | Claude Code |
| (c) 로직 재현 | 발주 시 지정 |

다음 두 유형은 양 채널에 동일 발주하여 결과를 대조한다(이중화).
- **전수 열거** — 문안·라우트·컬럼·테이블 목록 등 "빠짐없이"가 목적인 조사
- **파괴적 작업 전 판정** — 삭제 범위, 접근제어, 스키마 변경

이중화 발주를 받은 채널은 상대 채널의 결과를 묻거나 참조하지 않는다.
대조와 합집합 판정은 PM이 수행한다.

### 세션 시작 상태 보고

작업 착수 전, 첫 응답에서 다음을 보고합니다. 하나라도 확인 불가면 그 사실을 명시합니다.

1. 현재 모델·추론 강도
2. `git branch --show-current` 와 `git status --short` 결과
3. DB 접속이 필요한 작업이면 대상 ref와 키 유형
   - `.env.local` = dev 기본 / `.env.prod.local` = prod
   - Supabase CLI는 prod 링크·미인증 상태입니다. 링크 기본값을 신뢰하지 않습니다.
4. 직전 세션에서 넘어온 미커밋 변경이 있는지

**상태 보고 없이 파일 수정에 착수하지 않습니다.**
자기 작업 결과를 보고할 때도 실행 결과로 확인한 사실만 적습니다.
수행 여부가 불확실하면 `git status`로 확인한 뒤 보고합니다.

## 발주 명명 규칙

명령문 첫 줄에 다음 헤더가 붙습니다.

    [CC-##] 제목        ← Claude Code 수신
    [CX-##] 제목        ← Codex 수신
    [OW-##] 제목        ← 오너 수신
    수신: <채널> | 권한: <범위> | 산출물: <형태>

- **헤더의 수신처가 자기 채널이 아니면 수행하지 않고 오너에게 알립니다.**
- 헤더가 없는 지시는 수신처를 오너에게 확인한 뒤 착수합니다.
- 하나의 발주는 한 채널에만 배정됩니다. 다른 채널의 작업이 필요하면
  임의로 수행하지 말고 별도 발주가 필요함을 보고합니다.

## 실행 환경 (v2.2 신규)

### 검색 도구

- **`rg` / `findstr` / `Select-String` 을 사용하지 않습니다.**
  PowerShell 경유 `rg` 에 한글 패턴을 넘기면 오류 없이 0건을 반환하는
  침묵 실패가 확인됐습니다(2026-08-07).
- 모든 검색은 **Node `fs.readdirSync` 재귀 순회 +
  `readFileSync(p,'utf8')` + 정규식**으로 수행합니다.
- 제외 경로와 순회한 파일 총 개수를 보고에 명시합니다.
- 과거 `rg` 로 얻은 검색 결과는 전부 재검증 대상입니다.

### 터미널

- CMD 기본 코드페이지는 CP949 입니다. 착수 시 `chcp 65001` 로
  UTF-8 전환합니다.
- git 조작은 CMD 로 합니다. PowerShell 은 실행 정책 제약이 있습니다.

### 병렬 작업 중 근거 고정

- Codex 가 동시에 `src/` 를 편집 중이면 행번호가 이동합니다.
  **"현재 배포된 코드"를 묻는 발주는 `git show HEAD:<path>` 로
  근거를 고정**하고, 작업 트리를 읽었는지 HEAD 를 읽었는지
  보고에 명시합니다.

## 절대 원칙

- 파일은 **항상 읽고 나서** 판단합니다. 구조를 가정하지 않습니다.
- 지시받지 않은 파일은 수정하지 않습니다.
- 확인 못 한 항목은 "미확인"으로 명시합니다. 추측으로 채우지 않습니다.
- **오너의 화면 관찰이 코드 분석보다 우선합니다.** 코드 분석과 다르면 관찰을 부정하지 말고 차이의 원인을 조사합니다.

### 전제 반증 의무 (필수)

PM 명령문에 포함된 전제 — 파일 경로, 기존 값, 컬럼명·타입, 함수 존재 여부 — 가 **실측과 다르면 작업을 계속하지 말고 즉시 정지·보고합니다.**

- 전제가 틀린 채로 진행해 만든 산출물은 폐기 대상입니다. 진행보다 정지가 항상 옳습니다.
- 보고에는 "명령문의 전제 X / 실측 결과 Y / 근거 (d) 파일경로:행번호"를 명시합니다.

### 해법 우선 원칙

제약·한계·확인 불가 항목은 그 자체로 보고를 마치지 않습니다. 반드시 **획득 수단을 함께 제시**합니다.

- 수단이 있으면: 실행 가능한 명령·쿼리·절차를 즉시 제시
- 수단이 여럿이면: 권고안 1개를 지정하고 대안은 짧게 병기
- 수단이 정말 없으면: "무엇이 갖춰지면 가능해지는지"를 명시

위험을 보고할 때도 같습니다. 위험 서술과 **대응안**을 한 묶음으로 냅니다.

### 대조군 원칙

판별 테스트를 설계할 때 **"두 조건이 서로 다른 결과를 내는가"를 먼저 확인**합니다.

- 대조군 없는 테스트는 통과해도 아무것도 증명하지 않습니다.
- 수정 전 실패 → 수정 후 성공이 확인돼야 검증입니다.
- 대조군을 만들 수 없으면 그 사실을 보고하고, 무엇이 갖춰져야 검증 가능한지 제시합니다.

## 보고 시 근거 표기 (필수)

- `(a)` 실제 브라우저 렌더링·상호작용 확인
- `(b)` DB 조회 결과
- `(c)` 코드 로직 재현 또는 스크립트 계산 — **✅로 표기하지 않습니다.** "로직 재현"임을 명시합니다.
- `(d)` 파일 실측

코드 근거에는 `파일경로:행번호`를 붙입니다.

## DB 조회 키

- **service_role**: RLS 우회. 전수 집계·정합성 조사용. **화면 표시 판단의 유일 근거로 쓰지 않습니다.**
- **anon**: RLS 적용. **화면 동작·권한 판단은 반드시 이 키로.**
- 보고에 프로젝트(ref)와 키 유형을 항목마다 명시합니다.
- 예: `certifications`는 anon에게 `is_active = true` 행만 보입니다.

### 임의 SQL 실행 수단 없음 (2026-08-13 실측)

- 이 환경에는 `pg`·`postgres` 드라이버가 없고, PostgREST 에 노출된
  RPC 가 0건이며, `.env` 파일에 DB 접속 문자열이 없고,
  Supabase CLI 도 미인증입니다.
- 따라서 `information_schema`·`pg_constraint`·`pg_policies`·
  `pg_class.relrowsecurity` 를 직접 조회할 수 없습니다.
- 대체 수단: **PostgREST OpenAPI 정의(`GET /rest/v1/`, service_role)**
  로 컬럼·타입·NOT NULL 을, **행동 프로브**(anon 과 service_role 의
  결과 차이)로 RLS 효과를 판정합니다.
- 대체 수단으로 얻지 못한 항목은 **"미검증"으로 명시하고 오너가 SQL
  Editor 에서 실행할 쿼리를 함께 제시**합니다. 정지하지 않습니다.

## 환경

- prod Supabase: `sbketzgadjvzedbayesc`
- Supabase dev: `jgweeoeikhdjcgkitjfl` (Singapore, Free)

  ※ 2026-08-14 실측 정정. prod `sbketzgadjvzedbayesc` 는 Seoul 이다.

- 로컬 저장소: `C:\Kinepia\kinepia` / GitHub: `FassTLee/FassTLee`
- 개발자 user_id `4b3089c3-f85b-56ec-99e1-1c959ba4f878`는 dev와 prod가 **동일**합니다. dev 분석 시 제외 필터를 쓰면 안 됩니다.
- 인증: NextAuth. `profiles(id uuid)`가 정본이며 `session.user.id === profiles.id`입니다.
- 콘텐츠 JSON은 `docs/source-json/{자격증}/` 하위에 위치합니다.

### 환경변수 파일 구조 (2026-07-30 `3dcf4ec` 기준)

- `.env.local` — **dev 기본값**. 미지정 시 항상 dev로 동작합니다.
- `.env.prod.local` — prod 3키. **Next.js가 인식하지 않는 파일명이며, 이것이 격리의 목적입니다. 파일명 변경 금지.**
- `.env.development.local`은 **더 이상 존재하지 않습니다.** 이 파일을 참조하는 지시나 문서가 있으면 낡은 것입니다.
- 스크립트 3종은 `KINEPIA_TARGET` 가드가 있습니다. 미지정=dev, prod는 `--confirm-prod` 명시적 opt-in.

### Supabase CLI 상태 (2026-07-31 실측)

- CLI는 **prod(`sbketzgadjvzedbayesc`)에 링크**되어 있고 `supabase/config.toml`은 없습니다.
- CLI는 **미인증** 상태입니다(`~/.supabase/access-token` 없음).
- DB 작업 시 **대상 ref를 먼저 확인하고 dev를 명시**합니다. 링크 기본값을 신뢰하지 않습니다.

## 스키마 스냅샷

`docs/snapshot/` 하위에 스키마·코드 스냅샷 CSV가 있습니다. 파일명 규칙은 `{항목}_{env}_{YYYYMMDD}.csv`입니다.

| 파일 | 내용 |
|---|---|
| `columns_{env}` | 테이블·컬럼·타입·nullable·default |
| `columns-notnull_{env}` | NOT NULL 확정판 (information_schema 기준) |
| `constraints_{env}` | PK/FK/UNIQUE |
| `constraints-full_{env}` | 제약 전수 + `ON DELETE` 규칙 |
| `rls_{env}` | RLS 활성화 여부(행동 프로브) |
| `rls-policies_{env}` | RLS 정책 전수 (pg_policy 기준) |
| `rowcounts_{env}` | 테이블별 행 수 |
| `env-refs` | 코드가 참조하는 `process.env` 키 + 파일:행 |
| `api-routes` | API 라우트 + `getServerSession` 유무 + 사용 키 |
| `localstorage-keys` | localStorage 키 + 읽기/쓰기 위치 |
| `schema-diagram_{env}.svg` | 관계 다이어그램 |

**⚠️ 스냅샷은 방향 설정용입니다. 명령문의 전제로 쓸 값은 실측으로 재확인합니다.**
스냅샷은 채취 시점의 사진이며 정본이 아닙니다.

- 실데이터 행 값은 스냅샷에 담지 않습니다. 스키마 메타데이터와 코드 위치 정보만 담습니다.
- 2026-07-31 실측: dev·prod **컬럼 구성 완전 동일**(49테이블/495컬럼, 차이 0건).
- 단 제약 레벨에는 차이가 있습니다. 예: `profiles_certification_id_fkey`가
  prod=SET NULL / dev=NO ACTION. **dev 검증 결과를 prod에 그대로 적용하지 않습니다.**

## DDL·마이그레이션

- Supabase JS로 ALTER TABLE 불가. **SQL 파일로 작성해 오너에게 전달합니다.**
- SQL 작성 전 `information_schema.columns`로 실제 컬럼명·타입 확인. 확인된 타입을 SQL 주석에 명시합니다.
- 명명: `YYYYMMDD_설명.sql` (8자리, 시간부 없음). 작성 전 기존 파일명 확인.
- 같은 테이블·행을 다루는 기존 파일이 있는지 확인하고 대조합니다.
- 멱등 처리 필수: `IF NOT EXISTS`, 제약은 `DO $$ pg_constraint 확인 $$`, UPDATE는 조건절 포함.
- **신규 테이블은 RLS를 함께 설정합니다** (미설정 시 anon 무제한 접근).
- 여러 DDL은 `BEGIN; ... COMMIT;`으로 묶습니다.
- 적용 순서는 dev 후 prod. **`Success. No rows returned`는 적용 확인이 아닙니다** → 별도 검증 쿼리 필수.
- **PM 이 제시하는 SQL 블록에는 환경 표기가 붙습니다.**
  `[실행:dev]` / `[실행:prod]` / `[실행:dev→prod]` / `[실행:양측]` /
  `[참고]` / `[파일]`. 환경 표기가 없는 SQL 블록은 불완전한 산출물이며,
  파일로 저장하지 않고 PM 에게 확인을 요청합니다.

### upsert 주의

Supabase JS는 `column = column + 1` 같은 SQL 증분식 upsert를 지원하지 않습니다. 카운터 성격의 컬럼은 리터럴 값으로 덮어써지지 않도록 select-then-insert/update 패턴으로 구현합니다.

## 반복 함정

- `courses.title` / `certifications.name` — 테이블마다 명명이 다릅니다.
- 배열·UUID 캐스팅: `text[]` vs `name[]`, `uuid` vs `text` 명시적 캐스팅.
- `chapter_stats.user_id`는 text, `profiles.id`는 uuid → `p.id::text` 캐스팅.
- **`user_id` 컬럼에 무엇이 들어 있는지 반드시 확인합니다.** 테이블마다 uuid / text / email이 섞여 있을 수 있습니다. 매칭 조건 작성 전 실제 저장 값을 확인합니다.
- **`profiles.id`를 참조하는 FK는 대부분 `ON DELETE NO ACTION`입니다.** 자식 행이 있으면
  `profiles` 삭제가 FK 위반(23503)으로 거부됩니다.
- 과목→자격증 매핑 정본은 `course_certifications`. `courses.certification_id` 직접 컬럼은 공유 course를 누락합니다.
- `.single()`은 0행일 때도 PGRST116을 던집니다. 0행 가능성이 있으면 `.maybeSingle()`.
- `ORDER BY` 없는 SELECT의 순서에 로직이 의존하면 안 됩니다.
- `information_schema.tables`는 신규 테이블을 누락할 수 있습니다 → `pg_tables` 교차 확인.
- 신규 course 생성 시 `course_certifications` 매핑까지 한 트랜잭션으로.
- 서버 쓰기는 `supabaseAdmin`(service_role). **service_role은 RLS를 우회하므로 인증 검증이 코드 책임**이 됩니다.
- 답안 채점은 **서버 사이드만**. `answer_index`를 클라이언트 응답에 노출 금지.
- **DELETE/UPDATE가 0행에 매칭돼도 오류가 아닙니다.** 쓰기 검증은 반드시 영향 행 수를 확인합니다.
- **`.catch(() => 기본값)`으로 예외를 삼키지 않습니다.** 실패가 조용히 통과하면 파손을 발견할 수 없습니다.

## 정적 자산 주의사항

- `public/` 하위 파일은 코드가 경로로 직접 참조합니다. 삭제·이름 변경 시
  `src/app/layout.tsx`, `src/app/manifest.ts`, `src/middleware.ts` 등의 참조를
  먼저 실측하고 함께 수정합니다.
- `og-image.png`는 카카오톡·SNS 공유 썸네일입니다. 외부 캐시가 남아 파일을
  되돌려도 즉시 복구되지 않습니다.
- 압축 파일(`.zip`)·원본 소스는 저장소 커밋 대상이 아닙니다.

## 콘텐츠 작업 완료 기준

DB SELECT 확인만으로 완료 처리하지 않습니다. 신규 카드 샘플 3~5장을 **실제 브라우저로 렌더링**해 `explanation`/`key_points`가 의도대로 표시되는지 확인한 뒤에만 완료로 보고합니다.

## 콘텐츠 폐기

기존 courses/chapters는 `is_active = false`로 비활성화합니다. **DELETE 금지.** 행동 로그가 chapter_id만 갖고 FK가 없어, 원본이 사라지면 역추적이 불가능합니다.

## Git

- 기본은 읽기 전용 명령입니다: `git status`, `git diff`, `git log`, `git branch --show-current`.
- `git pull`은 **오너가 실행합니다.** 병렬 작업 중 미반영 변경이 보이면
  수정에 착수하지 말고 오너에게 pull을 요청합니다.
- 커밋·push는 오너의 **1회성 승인**이 있을 때만 실행합니다. 배포는 오너 단독입니다.
- 커밋 분할 기준: 문서 / 기능 수정 / 정적 자산은 서로 다른 커밋입니다.
  되돌리기 단위와 검증 단위가 다르기 때문입니다.
- 배포 전 확인 순서:
  1. 타입체크·빌드 (exit 0)
  2. 재현 테스트 — 실제 API 라운드트립 또는 DOM 실측
  3. **연관 UX 영향 범위 확인** (같은 화면의 다른 분기, 관련 화면, 상태 표시)
  4. `git diff --stat`으로 변경 파일 전수 보고
  5. 오너 승인 후 커밋 → push → 배포

## 모델·추론 강도 선택

작업 유형으로 판단합니다. 모델 라인업은 수시로 바뀌므로 **특정 모델명을 이 문서에 고정하지 않습니다.**

| 작업 유형 | 추론 강도 |
|---|---|
| 반복 조회, 원인이 명확한 단순 확인 | 낮음~중간 |
| 원인 불명 버그 추적, 여러 파일에 걸친 구조 분석 | 높음 |
| 아키텍처 판단, 데이터 구조 설계 | 최상위 |

- 기본 운용값은 **높음**입니다. 위 표의 상·하단 구간에 해당할 때만 조정합니다.
- 세대 교체 시 판단 기준은 유지하고, "현재 어떤 이름이 어느 구간에 해당하는지"만 그때그때 확인합니다.
